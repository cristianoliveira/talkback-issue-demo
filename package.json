{
  "name": "apimock",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "RECORD_MODE=r nodemon ./server.js",
    "start:backend": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3",
    "lodash": "^4.17.21",
    "talkback": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "prettier": "^2.3.2"
  }
}
